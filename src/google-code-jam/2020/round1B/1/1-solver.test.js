const { solveSingleCase } = require('./1-solver')

describe('2020, 1B', () => {
  describe('Expogo', () => {
    // it('2, 3', () => {
    //   expect(solveSingleCase(2, 3)).toEqual('SEN')
    // })
    // it('-2, –3', () => {
    //   expect(solveSingleCase(-2, -3)).toEqual('NWS')
    // })
    // it('3, 0', () => {
    //   expect(solveSingleCase(3, 0)).toEqual('WW')
    // })
  })
})
